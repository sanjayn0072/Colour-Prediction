# Antigravity General Rules

## Credentials Parsing
- When parsing credentials (such as passwords, usernames, phone numbers, or emails) provided in user prompt text (e.g. "password - Kumarnaveen."), identify and strip any trailing punctuation (like periods, commas, colons, semicolons, or quotes) that are part of sentence structure/grammar rather than part of the literal credential values.
